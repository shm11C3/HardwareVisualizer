name: Check Version Before Merge

on:
  push:
    branches:
      - master
      - release/**

  pull_request:
    branches:
      - release/**
      - master

jobs:
  check-version:
    uses: shm11C3/HardwareVisualizer/.github/workflows/check-version.yaml@021bd495b882b2c2d3805adfb6dadc15f0a979ae
